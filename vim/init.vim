syntax on
colorscheme molokai
set t_Co=256

set number
set smartindent

"setting  for tab
set expandtab
set tabstop=4
set shiftwidth=4

"if searching charactors is only lowercase letter, include uppercase letter in search result
set ignorecase
"if searching charactors is uppercase letter, distinguish from uppercase and lowercase.
set smartcase
"hightlight searched word
set hlsearch

" off auto new line
set tw=0
